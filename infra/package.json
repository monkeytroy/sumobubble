{
  "name": "@sumobubble/infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "docker compose -f ../docker-compose.yml up",
    "stop": "docker compose -f ../docker-compose.yml down",
    "reset": "docker compose -f ../docker-compose.yml down -v"
  }
}
